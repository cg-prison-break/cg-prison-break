# Prison Escape 3D Evaluation

## Usage

1. Install `uv`: https://docs.astral.sh/uv/getting-started/installation/
2. Place data in `data/no_shader` and `data/shader` subfolders
3. Run `evaluation.ipynb` notebook